# MarginPilot — pinned dependencies (Python 3.11).
# Nothing is added here without a reason recorded in CLAUDE.md's stack list.

# Web / actuator surface (webhook receiver)
fastapi==0.141.1
uvicorn==0.52.4

# Data + statistics (experiment engine, economics, evaluation harness)
numpy==2.3.5
pandas==2.3.3
scipy==1.17.1
statsmodels==0.14.6
scikit-learn==1.9.0

# Financial actuator — Razorpay test mode only
razorpay==2.0.1

# Dashboard (Day 10)
streamlit==1.62.0

# Tooling
pytest==9.1.1
python-dotenv==1.2.3

# LLM clients — used only inside src/agent/.
#
# Two, not one. CLAUDE.md's original skeleton called for a single client; the
# project has no Anthropic credentials available, so the agent runs on Gemini's
# free tier while the Claude path stays in the codebase. The reasoner is
# interface-swappable and which model produced a result is recorded with it.
anthropic==1.0.0
google-genai==2.20.0
