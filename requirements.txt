# MarginPilot — pinned dependencies (Python 3.11).
# Nothing is added here without a reason recorded in CLAUDE.md's stack list.

# Web / actuator surface (webhook receiver)
fastapi==0.141.1
uvicorn==0.52.4

# Data + statistics (experiment engine, economics, evaluation harness)
numpy==2.3.5
pandas==2.3.3
scipy==1.17.1
statsmodels==0.14.6
scikit-learn==1.9.0

# Financial actuator — Razorpay test mode only
razorpay==2.0.1

# Dashboard (Day 10)
streamlit==1.62.0

# Tooling
pytest==9.1.1
python-dotenv==1.2.3

# LLM client — used only inside src/agent/
anthropic==1.0.0
